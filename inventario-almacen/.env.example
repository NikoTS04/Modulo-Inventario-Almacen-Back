# Variables de Entorno para Despliegue
# Copiar este archivo y renombrar a .env para desarrollo local

# Database Configuration (Railway proveerá estos valores automáticamente)
SPRING_DATASOURCE_URL=jdbc:mysql://HOST:PORT/DATABASE?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
SPRING_DATASOURCE_USERNAME=usuario_mysql
SPRING_DATASOURCE_PASSWORD=password_mysql

# JPA Configuration
SPRING_JPA_HIBERNATE_DDL_AUTO=none
SPRING_JPA_SHOW_SQL=false

# Server Port (Railway asigna automáticamente)
SERVER_PORT=8080

# CORS - Orígenes permitidos (separados por coma)
# En Railway agregar la URL de Vercel: https://tu-app.vercel.app
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,https://tu-app.vercel.app

# Logging (en producción usar INFO o WARN)
LOGGING_LEVEL_ROOT=INFO
LOGGING_LEVEL_APP=INFO
