# Gradle caches and build artifacts
.gradle/
build/
**/build/

# Do not commit gradle distribution zips (can be very large)
# Keep gradle-wrapper.jar and gradle-wrapper.properties, but ignore the big binary distributions
gradle/wrapper/*-bin.zip
gradle/wrapper/*.zip

# Gradle libs produced by build
build/libs/
**/build/libs/

# Compiled classes and jars
**/*.class
**/*.jar

# IDEs
.vscode/
.idea/
*.iml

# Logs
logs/
*.log

# OS files
.DS_Store
Thumbs.db

# Environment
.env

# Misc
tmp/

# Keep wrapper jar and properties tracked (explicitly allow)
!gradle/wrapper/gradle-wrapper.jar
!gradle/wrapper/gradle-wrapper.properties
